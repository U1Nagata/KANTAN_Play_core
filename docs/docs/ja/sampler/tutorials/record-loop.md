# 演奏をRecする

RECモードでは、パッドを押したタイミングを記録し、同じ演奏をLoopとして繰り返せます。

最初はビートを鳴らしながら、サンプラーの2音だけをRecしてみましょう。

## 1. ビートを用意する

ビートが決まっていない場合は、内蔵Patternを1つ選びます。

[ビートを選ぶ](select-beat.md){ .md-button }

## 2. SAMPLERのRECモードにする

1. パートを`SAMPLER`にします
2. 4つの黒いモードボタンから`REC`を押します
3. ビートが止まっている場合は`Fn1`を押します

画面の演奏表示エリアが、Recした位置を示す表示に変わります。

<!-- IMAGE LOOP-01: SAMPLER + REC。Beat再生中で、まだRecイベントがない画面。 -->

## 3. パッド演奏を記録する

1. ビートを1周聴きます
2. 気に入ったパッドを1回押します
3. 次の周回で、別の位置にもう1音加えます
4. 何も押さずに次の周回を聴きます

自分が押さなくても同じ音が鳴れば、Recできています。画面には、Recした位置がパッドごとの印として表示されます。

<!-- IMAGE LOOP-02: 2〜4個のSamplerイベントが記録されたREC画面。 -->

## Loop Sectionで曲を展開する

Loop Sectionは、同じ音色やTempoのままRecとパターンビートだけを切り替える曲展開用の区間です。1〜4 Section使えます。

1. 下のダイヤルでメニューを開き、`Rec` → `Add Loop Section`を選びます
2. 現在の演奏を土台にするなら`Duplicate Current Loop`、パターンビートだけを残して新しく演奏するなら`Add Blank Loop`を選びます
3. 新しいSectionは現在位置の直後へ追加されます。ENC3でSectionを選び、Recへ音を足す／消すことで別展開を作ります

追加後は`S1 DUPLICATED AS S2`のようにコピー元と追加先が表示されます。停止中はすぐ切り替わります。
再生中は次のLoop先頭で切り替わり、予約中は`S1 > S3`のように表示されます。
現在Sectionへ戻すと予約を解除できます。ENC2はこれまでどおりPerformance Partを選びます。

`Delete Loop Section`は2回押しで確定します。最低1 Sectionは残り、再生中の削除は次のLoop先頭で実行されます。
`Clear Rec`は現在Loop Sectionの追加演奏だけを消し、パターンビート本体は残します。

## 4. 間違えたらUndoする { #undo }

直前のRecを戻すには、`Fn3`を短く押して離します。

Undoの履歴はPerformance Partごとに管理され、Loop Sectionを切り替えると破棄されます。

!!! warning
    `Fn3`を長押しすると、現在のパートにRecした演奏をまとめて消します。短押しのUndoと間違えないようにしてください。

## 特定のパッドだけを消す

RECモードで`Fn3`を押したまま、消したいパッドを押します。そのパッドのRecだけを削除できます。

この個別削除はUndoできません。対象パッドを確認してから操作してください。

## Recした演奏を一時的にMuteする

- `Fn2`を短く押す：現在パートのRecをMute / Unmute
- `Fn2`を押したままパッドを押す：そのパッドのRecだけをMute / Unmute

Mute中も、手で押した生演奏は鳴ります。記録済み演奏を止めて、その場の演奏へ切り替えられます。

## 別のパートを重ねる

Loopを止めずにパートを切り替え、同じ手順で音を重ねられます。

おすすめの順番：

1. `SAMPLER`
2. `BASS`
3. `MELODY`
4. `CHORD`

最初から全部使う必要はありません。サンプラーとBassだけでも十分に曲らしくなります。

## ビートなしでLoopを作る

ビートもRecもない状態では、最初にパッドを押した位置から記録が始まります。

1. `REC`モードで最初の音を押します
2. 好きな長さまで演奏します
3. `Fn1 END`を押します

最初の音からENDまでが新しいLoop長になります。

## 演奏を止める

- `Fn1`：Loopを停止。もう一度押すと先頭から再生
- 上のダイヤルを押す：すべての音とLoopを停止

[FXでLoopを変化させる](use-fx.md){ .md-button .md-button--primary }
